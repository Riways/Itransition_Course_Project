

@if(reviews == null || reviews.Count == 0)
{
    <MudText Typo="Typo.h2"> No reviews found</MudText>
}else{
    foreach(var review in reviews)
    {
        <Review/>
    }
}

@code {
    List<ReviewModel> reviews { get; set; }
}
